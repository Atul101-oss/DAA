// main.cpp
#include "LinkedList.h"

int main() {
    LinkedList myList;

    // Add elements to the list
    myList.append(1);
    myList.append(2);
    myList.append(3);
    myList.append(4);
    myList.append(5);

    // Display the elements of the list
    myList.display();

    return 0;
}
